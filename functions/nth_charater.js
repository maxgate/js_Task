// "use strict";

function myfunction(a, n) {
  a = "vincent";
  const e = n-1;

  return a.slice(e, n);
  // a.slice().forEach(n => {
  // return n[5];

  // });
  //   return n;
  //   for ( n= a.length,  indexOf(), n++);
}
w = myfunction('a', 5);
console.log(w);
